[project]
name = "arcadify-shell"
version = "0.1.0"
description = "Fullscreen Arcadify maintenance launcher"
requires-python = ">=3.9"

[project.scripts]
arcadify-shell = "arcadify_shell.app:main"

[tool.arcadify.system-dependencies]
debian = ["python3", "python3-tk", "python3-pil.imagetk"]
ubuntu = ["python3", "python3-tk", "python3-pil.imagetk"]
